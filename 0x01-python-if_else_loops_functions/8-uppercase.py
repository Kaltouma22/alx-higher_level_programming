#!/usr/bin/env python3
def uppercase(str):
    for c in s:
        if 'a' <= char <= 'z':
            upper = chr(ord(c) - 32)
            print("{:c}".format(ord(c) - 32), end="")
        else:
            print(char, end="")
    print()
